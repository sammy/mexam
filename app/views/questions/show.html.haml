.row
  .small-12.medium-12.columns
    %h4 
      Question: 
      %small=simple_format(@question.body)
      =image_tag(@question.question_image.url) if @question.question_image.url
    %hr

    %h4 Answers:

- @question.answers.each_with_index do |answer|
  .row
    .small-6.medium-8.columns
      - if answer.is_correct
        %span.success.radius.label=answer.body
      - else
        %span=answer.body

